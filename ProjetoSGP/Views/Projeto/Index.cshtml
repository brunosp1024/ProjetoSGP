@model IEnumerable<ProjetoSGP.Models.Projeto>

@{
    ViewBag.Title = "Index";
}

<button class="btn btn-default flutuar">@Html.ActionLink("+ Novo Projeto", "Cadastrar")</button>
<h2>Lista de Projetos</h2>

<div class="tabela">
    <table class="table table-condensed table-hover">
        <tr>
            <th>
                Id
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdProjeto)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Editar", new { id = item.IdProjeto }) |
                    @Html.ActionLink("Detalhes", "Detalhar", new { id = item.IdProjeto }) |
                    @Html.ActionLink("Excluir", "Excluir", new { id = item.IdProjeto })
                </td>
            </tr>
        }

    </table>
</div>

<hr />

<div>
    <button class="btn btn-default">@Html.ActionLink("Voltar", "Index", "Home")</button>
</div>

<style>

    .tabela {
        width: 850px;
        height: 380px;
        overflow: hidden;
        overflow-y: scroll;
        margin: 40px auto -5px auto;
    }

    tr th, tr td {
        color: black;
        text-align: left;
    }

    .flutuar {
        float: left;
        margin-bottom: 5px;
        position: relative;
        top: 50px;
    }

    h2 {
        position: relative;
        margin-right: 60px;
    }
</style>
